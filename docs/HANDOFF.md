# 引き継ぎ — ポートフォリオサイト実装

ローカルのClaude Codeで実装を始めるための要約。詳細は `docs/design-doc.md`、見た目は `docs/style-guide.html` をブラウザで開いて確認する。

---

## 何を作るか

自分の情報を集約するパーソナルサイト。ポートフォリオ（実績・スキル・略歴）+ Notionで管理しているブックマーク記事の表示。

- **クリエイティブ職向け**: 実績・スキル・略歴
- **非クリエイティブ職向け**: 作れるサイト種別 → 実績への導線
- UIは**管理画面（ダッシュボード）風**。白黒ベースで、プライマリボタンだけ彩色
- CMSは使わない。実績はMarkdown、自己紹介は静的
- 今後もAI開発をするので、デザインシステムの定義を先に固める

## 技術構成

| 項目 | 選定 |
|---|---|
| フレームワーク | Next.js (App Router) + TypeScript |
| スタイリング | Tailwind CSS v4 + shadcn/ui |
| 実績 | `content/works/*.md`（gray-matter + zodでfrontmatter検証） |
| 記事 | Notion API（`@notionhq/client`）+ ISR `revalidate = 3600` |
| フォーム | Server Action + Resend |
| デプロイ | Vercel |
| アイコン | lucide-react |

## サイトマップ

```
/                トップ（Overview・非クリエイティブ職向け）
/about           自己紹介・略歴・スキル詳細
/works           実績一覧
/works/[slug]    実績詳細（Markdown）
/articles        ブックマーク記事
/articles/[id]   記事詳細（一覧から開くとモーダル）
/contact         お問い合わせ
```

アプリシェル: 左サイドバー（`w-60`、各項目にlucideアイコン）+ 固定ヘッダー（`h-14`）+ メイン（`max-w-5xl`）。モバイルはサイドバーをSheetでドロワー化。

---

## デザインシステム（要点）

- **ニュートラル: zinc**（純グレーだと無機質なので、わずかに寒色へ振る）
- **primaryは1色だけ**。使ってよいのは「主アクション（お問い合わせ・送信）」「アクティブなナビ」「フォーカスリング」のみ。リンクは下線 + foreground、バッジはmutedで表現する
- **タイポ**: Inter（欧文）+ Noto Sans JP（和文）+ JetBrains Mono（数値・トークン）。本文基準14px、和文見出しに`palt`
- **`--radius` 1つ**を基準に、ボタン・バッジは相対値で導出
- **影は `shadow-sm` まで**。`shadow` はポップオーバーやドロワーだけ
- **ダークモードはv1スコープ外**だが、色は必ずトークン経由で書く（後から `.dark` を足すだけで済む状態を保つ）

トークンのCSSは `docs/style-guide.html` の最下部「トークン出力」からコピーできる。

### 外部リソースの使い分け

> **体験は借りる、トンマナは自前。**

| 区分 | 意味 | 該当 |
|---|---|---|
| A: そのまま入れる | shadcnと同じCSS変数を参照するもの | lucide-react、reui.io |
| B: 体験だけ借りる | コードは持ち込まず、挙動だけ真似て自前で組む | rareui.com、coss.com/ui、lucide-animated |
| C: 判断基準として読ませる | 実装後のレビューでエージェントに渡す | refactoring-ui-plugin、ui-skills.com、designsystemchecklist |

reicon.dev は不採用（lucideと重複。アイコンセットを2つ混ぜない）。詳細は design-doc.md §2.6。

---

## Notion連携（確認済み）

**カテゴリ = DBの分かれ方。** design / develop / other はプロパティではなく、それぞれ別データベース。3つのDB IDを環境変数で持ち、取得時にカテゴリを付与してマージする。

```
NOTION_TOKEN=
NOTION_DB_DESIGN=
NOTION_DB_DEVELOP=
NOTION_DB_OTHER=
RESEND_API_KEY=
```

**プロパティ**: タイトル(title) / URL(url) / 概要(rich_text) / 種別(select) / タグ(multi_select) / 会社名(rich_text) / ☆(checkbox) / 作成日時(created_time)

種別・タグの値はハードコードせず、取得結果から動的に集計する。**タグの語彙はDBごとに異なる**ので、絞り込みチップは選択中カテゴリのタグだけ出す。

### 実装で必ず踏むポイント

1. **サムネイルはページの1番目のブロックの画像**（`blocks.children.list` の `page_size: 1`）。画像がないケースのフォールバックは必須
2. ⚠️ **Notionのファイル署名URLは約1時間で失効する。** URLをそのままHTMLに埋めるとキャッシュされたページで画像が切れるので、`/api/thumb/[pageId]` を挟んで都度最新URLを引いて中継する
3. **本文ブロックはモーダルを開いた時に初めて取得**する（一覧生成時には取りに行かない）
4. ページネーション（`has_more` / `next_cursor`）を最初から実装。レート制限は平均3req/secなので並列度は3程度に絞る

### 記事UIの仕様

- ツールバー: カテゴリTabs / タグchip（複数選択OR）/ ☆のみ / リスト・カード切替
- 状態はURLクエリに持たせる（`?cat=design&view=card&tags=UI,AI&star=1`）
- 一覧は**リストが既定**。カードは16:9サムネイル付き
- 詳細モーダルは **Parallel Routes + Intercepting Routes** で `/articles/[id]` に紐づける。一覧から開けばモーダル、URL直打ちなら単独ページ、ブラウザバックで閉じる
- 空状態は2種類（絞り込み0件 / API失敗）。API失敗でもサイト全体は落とさない

---

## 実績（Works）

公開可のものは実名、クライアントワークは案件名をぼかす混在方針。画像が用意できないものが多いので、**サムネイルなしでも成立する**デザインにする。

frontmatterは design-doc.md §5.2 を参照。本文は「課題 → 取り組み → 成果」の3見出し。

初期投入するサンプル6件:

1. modulesss.com（実名・自主開発）
2. 賃貸物件検索サービスのCVR改善（ぼかし）
3. クリエイター向け工数管理SaaS（ぼかし）
4. カーボンクレジット品質評価サービス（ぼかし）
5. 制作会社マッチングメディアのリニューアル（ぼかし）
6. 不動産系サービスの店舗物件プラットフォーム（ぼかし）

---

## リポジトリ構成

```
.
├── CLAUDE.md                  # 下記の内容で作成
├── docs/
│   ├── design-doc.md          # 設計書（portfolio-design-doc.md をリネーム）
│   ├── style-guide.html       # スタイルガイド（portfolio-style-guide.html をリネーム）
│   ├── design-system.md       # design-doc.md §2 の要約（フェーズ0で作る）
│   └── resources.md           # design-doc.md §2.6 の切り出し（フェーズ0で作る）
├── content/works/*.md
└── src/
    ├── app/                   # 構成は design-doc.md §5.3
    ├── components/ui/         # shadcn（コピー&オウン）
    ├── components/            # AppShell, SkillItem, WorkCard, ArticleRow ...
    ├── lib/                   # notion.ts, works.ts, utils.ts
    └── styles/globals.css     # デザイントークン
```

---

## 実装フェーズ

各フェーズ後に動作確認してから次へ進む。

| # | 内容 | 完了条件 |
|---|---|---|
| 0 | Next.js + Tailwind v4 + shadcn/ui、トークン定義、CLAUDE.md / design-system.md / resources.md 作成 | トークンが反映されたButtonが出る |
| 1 | AppShell / SideNav / PageHeader、全ページの空実装 | ナビ遷移できる |
| 2 | トップ / About（スキル・略歴・作れるサイト） | 静的コンテンツが表示される |
| 3 | Markdownパイプライン、実績一覧・詳細、サンプル6件 | 6件が閲覧できる |
| 4 | Notion 3DB連携、ツールバー、リスト・カード、`/api/thumb`、詳細モーダル、ISR | 実データが絞り込み・両表示・モーダルで動く |
| 5 | Contactフォーム（Server Action + Resend + honeypot） | テスト送信が届く |
| 6 | メタデータ/OGP、レスポンシブ、refactoring-ui-plugin と ui-skills でUIレビュー、Vercelデプロイ | Lighthouse 90+、本番公開 |

**フェーズ4の最初のタスク**: Notion APIのレスポンスをダンプして、実プロパティ名と型を design-doc.md §5.1 の表と突き合わせる。

---

## 実装ステータス（2026-08-31 時点）

フェーズ0〜6を実装済み。`npm run build` / `npx tsc --noEmit` / `npm run lint` はすべて通る。

| # | 内容 | 状態 |
|---|---|---|
| 0 | Next.js 16 + Tailwind v4 + shadcn/ui、トークン定義、各ドキュメント | 完了 |
| 1 | AppShell / SideNav / PageHeader、全ページ | 完了 |
| 2 | トップ / About | 完了（文章は草案のまま） |
| 3 | Markdownパイプライン、実績一覧・詳細、サンプル6件 | 完了（本文は草案のまま） |
| 4 | Notion 3DB連携、ツールバー、リスト・カード、`/api/thumb`、詳細モーダル、ISR | 完了・実データ607件で確認済み（2回目の修正でモーダルを再設計） |
| 5 | Contactフォーム（Server Action + Resend + honeypot） | 実装完了。**テスト送信は未実施**（APIキー未設定） |
| 6 | メタデータ/OGP、アイコン、sitemap、robots、404/エラー画面 | 完了。**Lighthouse計測とVercelデプロイは未実施** |

### 決定したこと

- **アクセントカラー = Blue `hsl(221 83% 53%)`**（`#2563EB`）。トークンは `src/app/globals.css` に定義済み
- **Notion DB ID 3つ** — `docs/notion.md` のURLから取得して `.env.local` に設定済み

### 2026-08-31（2回目）の修正

| 指摘 | 対応 |
|---|---|
| モーダルが遅い | **原因はfaviconではなく、開くたびに記事607件を再取得していたこと**（Notionクエリ1回≈630ms × 8ページ ≈ 3秒）。Parallel/Intercepting Routes をやめ、一覧のデータで開くクライアントモーダルに変更。**開く時間はゼロ**、一覧に無い本文だけ `/api/articles/[pageId]` から約0.2秒で差し込む（スケルトン表示・クライアントキャッシュあり） |
| リスト/カード切替も同様に | 絞り込み・タグ・☆・表示切替・詳細の開閉をすべて History API でのURL書き換えに統一。ルート遷移も通信も起きない |
| 記事にすぐ飛べることが重要 | 行・カードは**面全体が外部リンク（別タブ）**。詳細モーダルは右端（カードは右上）のアイコンボタンでサブ導線に降格。URLを持たない記事だけ面全体が詳細を開く |
| 遷移先のアイコンは不要 | favicon（Google s2）を全廃。ドメインは文字で表示 |
| フォントを游ゴシックに | Webフォント（Inter / Noto Sans JP / JetBrains Mono）を全廃し、`--font-sans` を游ゴシックのOS標準スタックに。`--font-mono` は同じ字面 + `tabular-nums` |
| 左下のNextアイコン | `next.config.ts` に `devIndicators: false` |
| 画面遷移時のスケルトン | 各ルートに `loading.tsx` を追加（`src/components/skeletons.tsx` に実レイアウトと同じ骨格で定義） |
| Overview右上の問い合わせボタン | 削除。ヘッダー固定のボタンに一本化 |
| ヘッダーの問い合わせボタン | アクセントカラー（primary塗り）に。全ページ共通 |
| サイドバー下部の情報 | 削除 |
| 名前・肩書き | `Daisuke Tanaka` / `UI Designer / Frontend Engineer` |
| タイトル説明文の幅 | `max-w-2xl` → 100% |

### 2026-09-01（8回目）シェルの組み替え

| 指摘 | 対応 |
|---|---|
| ヘッダーを横幅100%に | シェルを「ヘッダー（全幅）→ その下に サイドバー + メイン」の縦積みに変更。サイドバーは `sticky top-14` |
| ロゴをヘッダーに | サイドバーから移設。ヘッダーの左余白を `lg:pl-4` にして、サイドバーの内側（`p-4`）と縦のラインを揃えている |
| サイドバーの Links をヘッダーへ | GitHub / Zenn をアイコンのみでダークモードボタンの左に配置。サイドバーの Links セクションは削除 |
| パンくずをコンテンツへ | `PageHeader` が先頭に描画する。ヘッダーからは削除 |
| 戻るボタンを削除し、パンくずで戻る | `PageHeader` の `backHref` を廃止。詳細ページでは**パンくずの親（Works）をリンクにして戻る導線を兼ねる** |
| 実績詳細の期間を削除 | `WorkMeta` を 種別 / 関与範囲 / スタック の3項目に（`period` は一覧カードには残る） |
| md内のテーブルをスタイリング | `.prose-work` に table / thead / th / td を追加。幅が足りないときだけ横スクロール |
| h2 の下線は不要 | `border-b pb-2` を削除し `font-bold` に |
| 箇条書きの丸をアクセントカラーに | `.prose-work ul > li::marker` と、スキルカードの丸を `--primary` に |
| お問い合わせボタンのホバー | `bg-brand-sweep`（`background-size: 220%`）を敷き、ホバーで `background-position` が 0% → 100% に流れる（500ms）。`motion-reduce` では明度変化にフォールバック |

**GitHub / Zenn のアイコンについて（要確認）**

`lucide-react` v1 は**ブランドアイコン（GitHub 等）を廃止している**ため、
意味の近い汎用アイコン（GitHub → `Code`、Zenn → `BookOpen`）を当て、
名前はツールチップと `aria-label` で補っている。
公式のブランドマークを使いたい場合は、SVGをいただければ `Logo` と同じ方式で差し込む。

### 2026-09-01（7回目）サイドバー開閉の作り直し

| 指摘 | 原因 | 対応 |
|---|---|---|
| 畳むと開閉アイコンとメニューアイコンの縦位置がずれる | 畳んだときだけロゴと開閉ボタンを縦積み（`flex-col`）にしていたため、ボタンが下へ落ちてナビの開始位置もずれていた | ロゴを収める枠を 0 幅にするだけにして、ヘッダー行は `h-8` 固定。**畳んでもロゴは非表示になるだけで、開閉ボタンもナビも動かない** |
| 開閉の途中で要素が潰れて大量に改行される | `aside` の幅に中身が追従していたため、幅が 64px の瞬間に中身がその幅で再レイアウトされていた | **中身は常に `w-60` で描画し、`aside` の幅だけをアニメーションさせて `overflow-hidden` で切り取る。** 中身が再レイアウトされないので折り返しが起きない。`whitespace-nowrap` も併用 |
| 出し入れが唐突 | — | `opacity` のクロスフェード（300ms の幅変化に対し、開くときは `delay-150` で文字を後から出す）。`motion-reduce:transition-none` でモーション低減設定を尊重 |

**寸法の根拠**（変えるときはここを崩さない）

```
レール幅 64px (w-16)          → 中心 32.0px
ナビのアイコン中心             → p-4(16) + px-2(8) + 15/2 = 31.5px
開閉ボタン中心 (size-8)        → p-4(16) + ロゴ枠(0) + 32/2 = 32.0px
```

### 2026-09-01（6回目 / `docs/feedback1/feedback2.md`）の対応

**ブックマークページの軽量化（重かった原因）**

全件（607件）をサーバーで取得し、そのままHTMLに埋め込んでいた。

| | 変更前 | 変更後 |
|---|---|---|
| HTML（gzip） | 82KB | **29KB** |
| HTMLに載る記事 | 607件 | 60件 |
| 残り547件 | 同上に含む | 描画後に `/api/articles` から取得（gzip 62KB・CDNキャッシュ1時間） |

取得のタイミングは2段構え。**ブラウザが空いた時間（`requestIdleCallback`）に静かに取りに行き**、
絞り込みや「もっと見る」で先に必要になったらその場で取りに行く。
件数とタグの語彙はサーバーで全件から集計して渡すので、**60件しか手元に無くてもタブの件数は607と正しく出る**。
絞り込み中に全件が揃っていないときは、不完全な結果を見せずスケルトンにする。

**その他**

| 指摘 | 対応 |
|---|---|
| Articles → Bookmark | ナビ・見出し・メタデータの表示名を変更（URLは `/articles` のまま） |
| パンくずのページ名を通常ウェイトに | `font-bold` を外した |
| ダークモードアイコンの余白 | ヘッダー右側を `gap-1.5` → `gap-3` |
| 閉じるアイコンをサイドバー内に | ヘッダーから移設。畳んだ状態でもレール内に残る |
| トップの屋号—肩書のアイブロウを削除 | `PageHeader` の `eyebrow` を外した |
| 説明文の改行 | `intro.body` を配列にし、「近年は」の手前で改行 |
| サイドバーの `Est. 2020 —` を削除 | 肩書きのみの1行に |
| ヘッダーのロゴ不要 | 削除（サイドバーのロゴに一本化） |
| サイドバーの modulesss 不要 | `externalLinks` から削除（GitHub / Zenn のみ） |
| Links を下寄せに | `mt-auto` で Links と屋号カードをまとめて下部へ。Menu が上に残る |
| Aboutのカードの角が直角 | **原因は `corner-shape: superellipse(4)`。** 角の曲率が四角に寄るため、半径の小さいカードでは直角に見えていた。カードから外し、ピルだけに残した |
| 詳細ページの戻るボタン | `PageHeader` に `backHref` を追加し、タイトルの左に矢印を置く形に変更 |
| お気に入りアイコンを黄色に | `--star` トークンを追加（primary とは別の意味を持つので独立させた） |
| 星マークが二連続 | ラベルを「☆のみ」→「お気に入りのみ」に（アイコンは残す） |
| モーダルを開くアイコン | `PanelRightOpen`（サイドバーの開閉と紛らわしい）→ `Expand` |
| タグの並び順 | **出現数順になっていたので修正。** `dataSources.retrieve` からスキーマを引き、**Notionに登録されている順**にした |

### 2026-08-31（5回目 / `docs/feedback1/feedback1.md`）の対応

**主語の変更（サイト全体）**

個人名ではなく **ITDクリエイティブ事務所** を主語にした。`src/lib/site.ts` が正。
支給されたロゴSVGを `src/components/logo.tsx` に取り込み、`fill` を `currentColor` に置換して
トークン経由で色が変わるようにしてある（`mark` でシンボルのみ切り出し可）。
連絡先も `info@itd-creative.com` に変更。

**コンテンツ**

| 指摘 | 対応 |
|---|---|
| 見出しの差し替え | 「デザインから運用まで、／プロダクトを一気通貫で」の2トーン。旧見出しの文は本文に降ろした |
| About上部が不要 | スタンス短文を削除。代わりに会社概要（代表 / 設立 / 連絡先 / 事業内容）を追加 |
| フリーランスを最後の略歴に | 現職の項目を削除し、`2020年4月 — ITDクリエイティブ事務所として独立` を最終項目にした |

**UI**

| 指摘 | 対応 |
|---|---|
| 英語フォントを Noto Sans に | `next/font/google` の Noto Sans（latin）を先頭に。和文はスタックの次点＝游ゴシックに落ちる |
| トップではパンくず不要 | トップは**ページ名のみ**（階層が無いのにパンくずを出さない）。下層は `ホーム > セクション名` |
| パンくずが無いと違和感 | ヘッダー左を [サイドバー開閉] + [畳んでいるときだけ屋号ロゴ] + [ページ名/パンくず] に再構成。どの状態でも左端が抜けない |
| 左右padding 40px | `lg:px-20` → `lg:px-10`（= 40px） |
| ピルはスムーズな角丸に | `pill` / `corner-smooth` ユーティリティ（`corner-shape: superellipse(4)`）。未対応ブラウザは通常の角丸にフォールバック |
| サイドバーの開閉 | 畳むと `w-14` のアイコンレール（ツールチップ付き）。状態は localStorage に保存（`lib/sidebar-store.ts`） |
| ダークモードボタン | ヘッダーに設置。**機能は未実装**（ツールチップに「準備中」と出るだけ） |
| サイドバー/ヘッダーに情報を | サイドバー: `Menu` / `Links`（GitHub・Zenn・modulesss）のセクション見出し + 最下部に屋号カード（Est. 2020）。ヘッダー: 開閉・ページ名・ダークモード・お問い合わせCTA |
| 無意味なホバーをやめる | スキルカード等、操作の無いカードからホバー効果を削除。`docs/design-system.md` にルールとして明記 |

**確認してほしい点**

- `corner-shape` は新しいCSSプロパティで、対応ブラウザ（Chrome 139+ 等）でのみ効く。
  未対応環境では通常の角丸になる（壊れはしない）
- 会社概要の「代表」欄は、いただいた原文どおり `ITDクリエイティブ事務所` / `田中 大介` の2行にしている。
  屋号と代表者名を別の行に分けたい場合は `src/lib/site.ts` の `company` を編集
- 略歴から現職の項目を落としている（「フリーランス時代を最後の略歴に」の指示による）
- 導入文の本文は主語を事務所に変えるために書き換えた。文面の確認をお願いしたい

### 2026-08-31（4回目 / `docs/feedback1/feedback.md`）の対応

**コンテンツ**

| 指摘 | 対応 |
|---|---|
| 自己紹介文の差し替え | 2トーンの見出し（1行目 foreground / 2行目 muted）+ 補足1文。`src/lib/profile.ts` の `intro` |
| 3つのカードは不要 | StatCard を削除（コンポーネントごと削除） |
| 作れるサイトも不要 | セクションと `siteTypes` を削除。`/works?type=` の絞り込み自体は残している |
| 次に直近の実績 | 導入 → 直近の実績3件 → スキル の順に再構成 |
| 具体的なスキル6つ | UIデザイン / フロントエンド / バックエンド / 施策立案 / AI開発 / その他。文面は指定どおり `src/lib/profile.ts` に格納 |

**UI**

| 指摘 | 対応 |
|---|---|
| パンくずのトップをアイコンに | `House` アイコン。下層では `ホーム > セクション名` |
| 問い合わせボタンを bold・少し大きく | `button.tsx` に `size="cta"`（`h-10 px-5 font-bold`）と `variant="brand"`（グラデーション）を追加。ヘッダー固定ボタンとトップのCTAで使用 |
| 戻るボタンは今のまま | `size="sm"` のまま触っていない |
| トンマナが無機質すぎる | 下記 |
| 左右paddingはPCで80px固定 | `max-w-5xl mx-auto` をやめ、`px-4 sm:px-6 lg:px-20`（=80px）に。`AppShell` の `GUTTER` 定数でヘッダーとメインを揃えている |

**トンマナ（参考画像3枚から抽出した方向性）**

- **2トーンの見出し** — 1行目 foreground / 2行目 muted。トップの導入で使用
- **ピル型のアイブロウ** — 枠線 + グラデーションのドット。全ページの `PageHeader` と各 `SectionHeading` に適用
- **グラデーション** — 色数は増やさず、primary(blue) → violet の**1系統だけ**。
  `--gradient-brand`（主アクション・ドット）/ `--gradient-surface`（全体の下地）/
  `--gradient-glow`（ページ上部のにじみ）/ `--tile-1`〜`--tile-6`（スキルのアイコンタイル）
- **ウェイトのメリハリ** — 見出しを `font-semibold` → `font-bold`、サイズも一段上げた
- **面をグラデーションで塗らない。** 点（ドット・アイコンタイル）と、ほとんど見えない下地にだけ使う

判断の記録: 「賑やかに」を色数を増やす方向でやるとトンマナが崩れるため、
**1系統のグラデーションを階調で使い分ける**方針にした。詳細は `docs/design-system.md`。

**Aboutページについて（要確認）**

スキルをトップに移したので、同じ内容を2箇所に置かないよう **About からはスキル一覧を外し、
スタンス + 略歴の構成にした**。Aboutにもスキルを残したい場合は戻せる。

### 2026-08-31（3回目）の修正

| 指摘 | 原因 | 対応 |
|---|---|---|
| モーダルの画像にタイムラグ | 一覧のカードは `/api/thumb/[id]`、モーダル本文は**生のNotion画像URL**を使っていて、別URL＝別キャッシュだった | モーダル本文の先頭画像も `thumbUrl()`（＝`/api/thumb/[id]`）で描画。URLを揃える窓口を `src/lib/thumb.ts` に集約 |
| （同上） | `/api/thumb` の `Cache-Control` が `max-age=0` で、**ブラウザがキャッシュしていなかった**。URLを揃えても取り直しになる | `max-age=3600, s-maxage=86400` に変更。CDNは1日、ブラウザはISRと同じ1時間 |
| リスト表示では画像を出していない | 先読みする対象が無い | 詳細ボタンのホバー / フォーカス / 押下で `prefetchThumb()`。加えて `requestIdleCallback` で先頭12件を先読み（一覧描画とJS初期化が終わってから／全件はやらない） |
| カードの幅が狭くなる | カードのルート `div` に `w-full` が無く、`<li class="flex">` の中でコンテンツ幅に縮んでいた。タイトルが短いほど顕著 | ルートに `w-full` を付与 |

**boneyard は採用しなかった。** `playwright` に依存し（ヘッドレスブラウザのバイナリを落とす）、
自動キャプチャは Vite プラグイン前提で、Next.js + Turbopack では
「dev サーバーを起動 → ヘッドレスブラウザで巡回」する CLI しか使えない。
今回のスケルトンは遷移時のもので、骨格は既知・各10〜20行で書ける。
`docs/resources.md` の持ち込み判断（3. 依存パッケージが増えるか）に照らして見合わないと判断した。

### 設計書からの変更点（実データに合わせて修正）

1. **Notionのプロパティ名・型が草案と違った**（`docs/design-doc.md` §5.1 を更新済み）
   - タイトル → 実際は **`名前`**
   - 種別は `select` ではなく **`multi_select`**
   - 会社名は `rich_text` ではなく **`multi_select`**、しかも **develop DBには存在しない**
   - develop DBにだけ **`言語`（multi_select）** がある → タグと同じ絞り込みプールに入れている
2. **サムネイルを一覧生成時に取りに行かない。** 記事が607件あり、1件ずつ `blocks.children.list` を叩くと
   レート制限（3req/sec）で200秒かかる。代わりに `/api/thumb/[pageId]` の中で1番目のブロックを引くようにし、
   ブラウザがカード表示で実際に見えている分だけ遅延読み込みする
3. **実績のfrontmatterに `summary` を追加。** サムネイルなしのカードを成立させるための説明文
4. **`hsl(var(--primary))` 形式は使っていない。** shadcn/ui v4 + Tailwind v4 ではトークンが完全な色値を持つため、
   `bg-primary` などのセマンティッククラスと `var(--primary)` で参照する（`CLAUDE.md` に明記済み）
5. **一覧は60件ずつ描画**（「もっと見る」で追加）。607件を一度にDOMへ出さないため

### 残っていること

- [x] **Notionトークンをgit履歴から削除済み** — create-next-app の初回コミットに `docs/notion.md` が
      含まれていたため、コミットを作り直して旧オブジェクトを prune した（`be929b1` → `5677b14`）。
      `.git` 内に平文は残っていない。`docs/notion.md` と `.env.local` は `.gitignore` 済みで、
      作業ツリーにのみ存在する。**リモートには一度も push されていない**
- [ ] **`docs/notion.md` をどうするか** — 値は `.env.local` に入っているのでファイル自体は不要。
      リポジトリ内に平文の鍵を置き続けたくなければ削除するか、リポジトリ外へ移す
- [ ] **Resendのテスト送信** — `RESEND_API_KEY` を `.env.local` に設定してから `/contact` で実送信を確認する
- [ ] **UIの目視確認** — ブラウザで各ページとモーダル・モバイル幅を確認する
      （こちらではブラウザを操作していないため未確認。特にモーダルの開閉と戻るボタンの挙動）
- [ ] **UIレビュー** — refactoring-ui-plugin と ui-skills を通す（`docs/resources.md` の区分C）
- [ ] **Lighthouse 90+ の計測**
- [ ] **Vercelデプロイ**、`NEXT_PUBLIC_SITE_URL` を本番ドメインに差し替え
- [ ] **文章の確認・書き直し** — 実績6件の本文、スキル・つくれるサイトの説明文（`src/lib/profile.ts`）
- [ ] **略歴の年表記** — `src/lib/profile.ts` の `timeline`。専門学校〜独立までの年が `—` のまま
- [ ] **`--font-mono` の扱い** — 「サイト全体を游ゴシックに」に合わせ、数値・ドメイン用の
      `--font-mono` も游ゴシック（`tabular-nums` で数字だけ揃える）にしている。
      日付やドメインを等幅フォントに戻したい場合は `globals.css` の1行を変えるだけ
- [ ] **サイドバーのリンク** — メールと modulesss.com のみ。GitHub / X を出すなら `src/components/side-nav.tsx`

## CLAUDE.md（そのままリポジトリ直下に置く）

```md
# ポートフォリオサイト

UIデザイナー / フロントエンドエンジニアの個人サイト。管理画面風の白黒UI。

## ドキュメント

- 設計の全体像: `docs/design-doc.md`
- デザインシステム要約: `docs/design-system.md` ← UIを書く前に必ず読む
- 外部リソースの使い分け: `docs/resources.md` ← 参考実装を探すとき / UIレビューのときだけ読む
- 見た目の確認: `docs/style-guide.html`（ブラウザで開く）

## 実装ルール

1. **色は必ずトークン経由で参照する。** コンポーネントに生の色値（hex / oklch / `bg-blue-500`）を書かない。`hsl(var(--primary))` の形で書く
2. **新しいUIはまず既存コンポーネントの組み合わせで検討する。** 足りないときだけ新規作成
3. **shadcnの追加は `npx shadcn@latest add` で行う。** 手書きしない
4. **外部コンポーネントは `docs/resources.md` の区分に従う。** 区分Bのものはコードを持ち込まず、挙動だけ真似て自前のトークンで組み直す
5. **primaryを使ってよいのは** 主アクション・アクティブなナビ・フォーカスリングだけ。リンクは下線 + foreground、バッジはmutedで表現する
6. **アニメーションは状態変化を説明するときだけ。** ページ遷移演出は付けない
7. 兄弟要素の間隔は margin ではなく親の `gap` で作る

## コマンド

- 開発: `npm run dev`
- ビルド: `npm run build`
- 型チェック: `npx tsc --noEmit`
```

---

## 最初にやること

1. このファイルと `design-doc.md` / `style-guide.html` を `docs/` に置く
2. `style-guide.html` をブラウザで開き、アクセントカラーを決める → 最下部「トークン出力」からCSSをコピー
3. `npx create-next-app@latest` → `npx shadcn@latest init` → コピーしたトークンを `globals.css` に貼る
4. 上記の CLAUDE.md をリポジトリ直下に作成
5. フェーズ0から順に進める

---

追記

アクセントカラーは選択肢の中の青にする
